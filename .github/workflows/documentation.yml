name: Doxygen to GitHub Pages

#on:
#  push:
#    tags:
#      - 'v*'

on: [push]

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v2

    - name: build doxygen sources
      uses: mattnotmitt/doxygen-action@v1
      with:
        working-directory: 'Projects/Sources/'
        doxyfile-path: './Doxyfile'
        enable-latex: true

    - name: upload artifacts
      uses: actions/upload-artifact@v1
      with:
        name: docs
        path: '${{runner.workspace}}/Documentation'

    #- name: upload latex artifacts
    #  uses: actions/upload-artifact@v1
    #  with:
    #    name: docs_latex
    #    path: '${{runner.workspace}}/Documentation/latex'
    #
    #- name: upload html artifacts
    #  uses: actions/upload-artifact@v1
    #  with:
    #    name: docs_html
    #    path: '${{runner.workspace}}/Documentation/html'

    #- name: deploy generated HTML
    #  uses: peaceiris/actions-gh-pages@v3
    #  with:
    #    github_token: ${{ secrets.GITHUB_TOKEN }}
    #    publish_dir: '${{runner.workspace}}/Documentation/html'
    #    enable_jekyll: false
    #    allow_empty_commit: false
    #    force_orphan: true
    #    #publish_branch: gh-pages
